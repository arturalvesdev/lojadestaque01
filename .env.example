# Copie para .env.local e preencha (Supabase → Settings → API)
# Este arquivo É commitado no git — use apenas como documentação de estrutura.
# Nunca coloque chaves reais aqui.

NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=seu-anon-public-key-aqui
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# Chave de serviço (somente servidor — nunca exposta ao browser)
# Usada em: lib/supabase/admin.ts → app/api/cart/route.ts
